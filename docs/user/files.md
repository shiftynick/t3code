# Browse project files

Open **Files** in the right panel to browse and preview files in the current project. Folders start
collapsed, and opening a file from search or chat expands its parent folders automatically.

In Git repositories, files matched by ignore rules are hidden by default. Use **Show ignored files**
in the file-browser toolbar to include them for the current panel. Git's own repository metadata
remains hidden.
